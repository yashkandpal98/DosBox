.MODEL SMALL

.STACK 64

KEYBOARD SEGMENT AT 40H
	ORG 1EH
	OFF DW ?
KEYBOARD ENDS


.CODE

MAIN PROC FAR
	MOV AX, KEYBOARD
	MOV ES, AX

   XYZ: MOV AH, 00H
	INT 16H

	MOV DI, OFF

	MOV ES:[DI], AL

	CMP AL, 1BH
	JE ABC

	MOV DL, ES:[DI]
	INC DL
	CMP DL, 5BH
	JNE DEF
	MOV DL, 41H
	JMP XXX
   DEF:	CMP DL, 7BH
	JNE XXX
	MOV DL, 61H
	JMP XXX
 
   XXX:	MOV AH, 02H
	INT 21H

	JMP XYZ

   ABC: MOV AX, 4C00H
	INT 21H
MAIN ENDP
END MAIN