.MODEL SMALL


.STACK 64


.DATA
	ONE DB 1, 5, 25, 7
	GRT DB 'GREATEST: ','$'
	LST DB 'LOWEST: ','$'


.CODE
MAIN PROC FAR

	MOV AX, @DATA
	MOV DS, AX

	LEA SI, ONE
	LEA DI, GRT
	
   XXX: MOV DL, [DI]
	MOV AH, 02H
	INT 21H
	INC DI
	MOV CL, [DI]
	CMP CL, '$'
	JNE XXX

	MOV AL, [SI]
	MOV BL, [SI]

	MOV CL, 4

   YYY: MOV DL, [SI]
	CMP AL, DL
	JG ZZZ
	CMP BL, DL
	JL WWW
	JMP ABC
	ZZZ: MOV AL, DL
	     JMP ABC
	WWW: MOV BL, DL
   ABC: INC SI
	DEC CL
	JNZ YYY

	MOV CL, AL

	MOV AL, BL
	MOV AH, 00
	MOV BL, 0AH
	DIV BL

	MOV BL, AH
	ADD AL, 30H
	MOV DL, AL
	MOV AH, 02H
	INT 21H

	MOV AL, BL
	ADD AL, 30H
	MOV DL, AL
	MOV AH, 02H
	INT 21H

	MOV DL, 0AH
	INT 21H
	INT 0AH

	MOV DL, 0AH
	INT 21H
	INT 0AH
	
	LEA BX, LST

	MOV DL, [BX]
   DEF: MOV AH, 02H
	INT 21H
	INC BX
	MOV DL, [BX]
	CMP DL, '$'
	JNE DEF

	MOV AH, 00
	MOV AL, CL
	MOV BL, 0AH
	DIV BL
	
	MOV BL, AH
	ADD AL, 30H
	MOV AH, 02H
	MOV DL, AL
	INT 21H

	MOV AL, BL
	ADD AL, 30H
	MOV DL, AL
	MOV AH, 02H
	INT 21H

	MOV AX, 4C00H
	INT 21H

MAIN ENDP
END MAIN